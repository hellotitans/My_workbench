# 我的生活台 · 访问方式交付

把工作台从「一个本地文件」变成「两种随时能打开的方式」。

## 交付物

| 方式 | 入口 | 适合场景 |
|---|---|---|
| 在线链接 | https://www.workbuddy.cn/space/d/8XbFZm5JLLOwVyGACRFakM | 换电脑、手机上查看；存书签即可 |
| 本地启动包 | `我的生活台启动包/Start.bat` | 自己电脑日常记录；可设开机自启 |

## 做了什么

1. **上线资料库**：把 `personal-workbench.html` 导入为资料库在线 page
   （node_block_id `8XbFZm5JLLOwVyGACRFakM`），登录 WorkBuddy 账号即可访问。

2. **本地启动包**：`我的生活台启动包/` 含 Start.bat、页面副本、使用说明。
   Start.bat 优先打开工作区主文件，保证永远是最新版；主文件缺失时自动退回同目录副本。

3. **存储容错改造**（顺手修的隐患）：原 `store.set` 没有异常捕获，在云端 iframe
   或隐私模式下 localStorage 不可写时会直接抛错卡死页面。现在会探测可写性，
   不可用时自动降级到内存存储，页面照常可用并提示「数据仅本次有效，请及时导出备份」。

## 需要知道的事

- **数据在浏览器本地，不在服务器**，别人打不开你的记录。
- **本地打开 与 在线链接 是两套互不相通的数据**。跨端搬运：A 处「导出 JSON」→ B 处「导入恢复」。
- 建议每周导出一次 JSON；不要清浏览器网站数据，清了本地记录会没。

## 验证

- 内联脚本 `node --check` 语法通过
- 浏览器实测：7 个模块、8 个页面全部渲染，存储读写正常，无初始化报错

## 后续可选项

- 若想多设备实时同步（而不是手动导 JSON），需把数据层改接资料库数据表，工程量大，可另行评估
- 若想免登录公开访问，可以再执行「发布」，得到公开链接（注意公网可见，别放隐私数据）
