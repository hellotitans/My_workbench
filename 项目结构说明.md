# 项目结构说明

> 最后整理：2026-09-23（清理前 1.79 GB → 整理后 18.3 MB）

## 顶层目录

| 路径 | 内容 | 是否入库 |
|---|---|---|
| `personal-workbench.html` | **唯一源码**，单文件工作台（CSS/JS/图标全内联，零外部依赖） | ✅ |
| `我的生活台启动包/` | 交付副本 `我的生活台.html` + `Start.bat`（全 ASCII）+ 使用说明 | ✅ |
| `设计稿/皮肤方案/` | A 深空指挥台 / **B 赛博霓虹（定稿）** / C 终端极客，各含 html + png | ✅ |
| `docs/` | 01 需求、02 技术、03 版本记录、04–06 V0.1 文档、07 可视化皮肤模块设计 | ✅ |
| `作品集/` | 01 个人生活台、02 AI 前沿日报 | ✅ |
| `.workbuddy/` | 本地工作区（记忆、脚本、备份、归档），**不入库** | ❌ |
| `README.md` / `overview.md` / `LICENSE` / `.gitignore` | 门面与授权 | ✅ |

## `.workbuddy/` 内部约定

```
.workbuddy/
├─ test-m6.js  test-m9.js      回归测试（CDP 驱动无头 Edge）
├─ shot-mockups.js             截图
├─ check-mockups.js  check-b10.js  布局/样式体检
├─ png-luma.js                 光晕能量量化（替代肉眼）
├─ extract-script.js           抽 <script> 做语法检查
├─ clean-b10.js                剥离预览管线注入属性
├─ scan-project.js             目录体积诊断
├─ rm-browser-profiles.js      清理废弃浏览器 profile
├─ memory/
│   ├─ MEMORY.md               长期记忆（已蒸馏，权威）
│   ├─ 2026-09-2x.md           最近工作日志
│   └─ archive/                已蒸馏归档的旧日志
├─ archive/2026-09-23-tidy/    一次性脚本 / 过程截图 / 结果 json+txt
├─ snapshots/                  历史版本截图留痕
├─ backups/                    重大改动前的 HTML 备份
└─ tmp-soundfield/             声场球原型（独立 git 仓库）
```

**根目录只放常用工具脚本**，一次性脚本与过程产物一律进 `archive/<日期>-<主题>/`。

## 清理记录（2026-09-23）

- 删除 22 个废弃无头浏览器 profile（`cdp-*`、`edge-b10`），释放 **1.77 GB**（占原体积 97%）。
  现脚本已改用系统 `TEMP` + 时间戳目录，**浏览器 profile 不得落在 `.workbuddy/`**
- 根目录从 160 个散落文件收敛到 13 个；148 个一次性产物归集进 `archive/`
- 8 个旧日志蒸馏进 `MEMORY.md` 后移入 `memory/archive/`
- 修正 `我的生活台启动包/我的生活台.html`：清掉 745 处预览注入属性，与主文件字节一致

## 关键约束（改动前必读）

1. **设计稿 `设计稿/皮肤方案/B-赛博霓虹.html` 是样式取值的唯一依据**——改样式先 grep 源码数值，不凭记忆
2. 改完主文件必须同步副本：`fs.copyFileSync` 后比对字节数（副本曾混入 745 处注入属性）
3. 提交后再 `present_files`；事后用 `git show HEAD:<path>` 统计 `data-page-node-id` 确认为 0
4. 推送优先 SSH：`git push git@github.com:hellotitans/My_workbench.git main`
